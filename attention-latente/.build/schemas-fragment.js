{
      "schema-01": {
        "mha": { title: "MHA — la référence coûteuse", body: `<p>L'attention multi-têtes classique donne à chacune de ses <em>n_h</em> têtes sa propre projection clé et valeur. Le cache pèse <code>2·n_h·d_h</code> éléments par token et par couche — 32 768 pour 128 têtes de dimension 128. C'est le maximum de qualité, mais aussi le maximum de mémoire à relire à chaque token généré. Toute la famille MQA/GQA/MLA existe pour réduire ce chiffre sans trop toucher à la qualité.</p>` },
        "gqa": { title: "GQA — le compromis dominant", body: `<p><em>Grouped-Query Attention</em> partage un jeu clé/valeur entre <em>g</em> groupes de têtes de requête. Avec 8 groupes sur 128 têtes, le cache tombe à 2 048 éléments/token tout en préservant l'essentiel de la qualité MHA. C'est le défaut de Llama, Qwen, Mistral, Gemma — souvent obtenu par <em>uptraining</em> d'un checkpoint MHA sur ~5 % du budget. Mais réduire davantage y revient toujours à partager plus de têtes : on <em>jette</em> de l'information plutôt que de la comprimer.</p>` },
        "mqa": { title: "MQA — trop loin", body: `<p><em>Multi-Query Attention</em> (Shazeer 2019) pousse le partage à l'extrême : une seule tête clé/valeur pour toutes les requêtes, soit le plus petit cache possible (256 éléments/token ici). Le prix est une perte de qualité mesurable et une instabilité d'entraînement sur les gros modèles. MQA a montré la voie mais s'est révélé trop agressif — GQA est né pour l'adoucir.</p>` },
        "mla": { title: "MLA — comprimer, pas trancher", body: `<p>MLA ne partage pas de têtes : elle projette clés et valeurs dans un <strong>latent conjoint</strong> de dimension 512, plus une petite clé RoPE partagée de 64, soit <strong>576 éléments/token</strong> — à peine plus qu'une GQA à 2 groupes. Mais là où GQA à cette taille dégrade la qualité, MLA <em>dépasse</em> MHA. La compression est apprise, pas structurelle : l'entraînement choisit les directions à conserver. <mark>Petit cache ET meilleure qualité : c'est le déplacement du front de Pareto.</mark></p>` }
      },
      "schema-02": {
        "down-proj": { title: "Down-projection W^DKV", body: `<p>Pour chaque token, la matrice <code>W^DKV</code> comprime l'état caché (dimension du modèle, ~5 120) en un unique vecteur latent <em>c_KV</em> de dimension 512. C'est une projection descendante de rang faible : on suppose que l'essentiel de l'information clé/valeur du token tient dans ce sous-espace réduit. Rien d'autre n'est conservé pour le cache.</p>` },
        "latent-cache": { title: "c_KV — le seul objet mis en cache", body: `<p>Le latent <em>c_KV</em> (512 éléments) est la <strong>seule</strong> chose écrite dans le cache, au lieu des clés et valeurs pleines. C'est la source du gain mémoire : un ordre de grandeur sous le MHA. Point crucial de la section 4 : à l'inférence, ce latent n'est pas décompressé — l'attention opère directement dessus grâce à l'absorption des matrices. <mark>Le cache latent n'est pas une version comprimée de l'état : il <em>est</em> l'état.</mark></p>` },
        "up-proj": { title: "Up-projections W^UK, W^UV", body: `<p>Deux matrices montantes reconstruisent, à partir du latent, les clés (<code>W^UK</code>) et valeurs (<code>W^UV</code>) pleines. Naïvement, cette reconstruction à chaque pas de décodage annulerait le gain. Mais à l'inférence ces matrices se <em>replient</em> dans des matrices voisines (schéma 4) et ne sont jamais exécutées explicitement. Elles ne servent réellement qu'à l'entraînement, où K/V pleins sont matérialisés pour les gradients.</p>` },
        "query": { title: "La voie requête aussi comprimée", body: `<p>DeepSeek comprime également les requêtes en un latent (dimension 1 536) via <code>W^DQ</code>, puis les reconstruit par <code>W^UQ</code>. Ce n'est pas un gain mémoire (les requêtes ne sont pas cachées) mais une réduction du coût d'activation à l'entraînement, et c'est <code>W^UQ</code> qui absorbe <code>W^UK</code> à l'inférence. La compression latente est donc appliquée symétriquement aux deux côtés du produit d'attention.</p>` }
      },
      "schema-03": {
        "content-path": { title: "Voie contenu — absorbable", body: `<p>Les dimensions « contenu » des clés et requêtes ne portent aucune information de position. Elles sont compressées dans le latent <em>c_KV</em>, de rang faible, et surtout <strong>absorbables</strong> : <code>W^UK</code> peut se fondre dans <code>W^UQ</code> hors ligne, car aucune rotation position-dépendante ne s'intercale. C'est cette voie qui permet le calcul-dans-le-latent de la section 4.</p>` },
        "position-path": { title: "Voie position — découplée", body: `<p>De petits vecteurs dédiés <em>k^R</em> et <em>q^R</em> (64 dimensions par tête) portent <em>uniquement</em> le signal RoPE. Ils ne passent pas par la compression latente et subissent leur rotation normalement. La clé RoPE est <strong>partagée</strong> par toutes les têtes et cachée à part. En les isolant, DeepSeek confine la partie non-absorbable à un petit budget fixe, laissant la voie contenu pleinement compressible.</p>` },
        "rope-break": { title: "Concaténation et produit", body: `<p>Avant le produit scalaire, voie contenu et voie position sont concaténées. Le score d'attention combine ainsi une part absorbable (contenu, dans le latent) et une part explicitement calculée (position, après rotation). C'est ce montage à deux voies qui réconcilie deux exigences qu'on croyait incompatibles : encoder la position par RoPE <em>et</em> ne cacher qu'un latent.</p>` },
        "cache-store": { title: "Ce que le cache stocke : 576", body: `<p>Le cache MLA par token et par couche contient exactement deux objets : le latent conjoint <em>c_KV</em> (512) et la clé RoPE découplée partagée (64), soit <strong>576 éléments</strong>. À comparer aux 32 768 d'un MHA à 128 têtes — une réduction de 93,3 % annoncée par DeepSeek-V2<a class="cite" data-cite="4" href="#source-4">4</a>. La position n'ajoute qu'un terme constant et partagé, pas un cache par tête.</p>` }
      },
      "schema-04": {
        "latent-compute": { title: "Le chemin naïf (entraînement)", body: `<p>La voie intuitive : depuis le latent <em>c_KV</em>, remonter aux clés pleines via <code>W^UK</code>, puis calculer <code>q·kᵀ</code>. Correct, mais coûteux à l'inférence : il faudrait décompresser tout le contexte à chaque pas de décodage. C'est le chemin utilisé à l'entraînement, où les K/V pleins servent au calcul des gradients — pas au service.</p>` },
        "absorb-keys": { title: "Absorption côté clés", body: `<p>Le score <code>(W^UQ c_Q)·(W^UK c_KV)</code> se réécrit <code>c_Q · [(W^UQ)ᵀ W^UK] · c_KV</code>. Le produit entre crochets est une matrice <strong>constante</strong>, calculable une fois hors ligne. <code>W^UK</code> est donc <em>absorbée</em> dans la projection de requête : on calcule le score directement entre requête et latent, sans jamais reconstruire la clé pleine. Le surcoût de la up-projection disparaît.</p>` },
        "absorb-values": { title: "Absorption côté valeurs", body: `<p>Symétriquement, la valeur pleine <code>v = W^UV c_KV</code> n'apparaît que multipliée par la matrice de sortie <code>W^O</code>. On absorbe donc <code>W^UV</code> dans <code>W^O</code> : l'attention pondère les latents, et la reconstruction des valeurs est repoussée dans une projection déjà présente. Là encore, aucun calcul supplémentaire au décodage.</p>` },
        "cache-is-state": { title: "Le cache est l'état", body: `<p>Conséquence des deux absorptions : à l'inférence, on ne matérialise jamais K et V pleins. L'attention opère <em>directement</em> sur les latents cachés. <mark>Le cache latent n'est pas une version comprimée de l'état à décompresser — il <em>est</em> l'état sur lequel l'attention opère.</mark> C'est ce qui distingue MLA d'une simple compression greffée. Note : vLLM et FlashMLA implémentent aussi une variante « naive » (recalcul) selon l'arbitrage calcul ↔ mémoire.</p>` }
      },
      "schema-05": {
        "frontier": { title: "Deux frontières", body: `<p>La courbe teal est le compromis qualité × mémoire qu'atteignent MHA et les variantes GQA : un arbitrage fixe le long duquel on glisse en changeant le nombre de groupes. La courbe carmine est celle que MLA rend accessible : à mémoire égale, une meilleure qualité. <mark>GQA <em>suit</em> le front ; MLA le <em>déplace</em>.</mark> C'est la différence entre optimiser un compromis et en changer la nature.</p>` },
        "gqa-point": { title: "Où tombe GQA", body: `<p>Les points GQA se situent sur la frontière fixe : GQA-8 offre presque la qualité MHA à 2 048 éléments/token ; GQA-2 descend en mémoire mais paie en qualité. On ne peut pas être à la fois plus petit et meilleur — le partage de têtes impose ce troc. L'ablation DeepSeek-V2 place GQA au mieux à parité avec MHA, jamais au-dessus.</p>` },
        "mla-point": { title: "Où domine MLA", body: `<p>MLA occupe le coin convoité : haut (qualité > MHA) et gauche (petit cache, 576 éléments/token). À l'échelle DeepSeek, c'est un facteur 3,5 de mémoire sous GQA-8, <em>avec</em> une qualité supérieure. La raison : un latent appris capture plus d'information par octet qu'un partage de têtes structurel — l'entraînement choisit les directions à garder au lieu de les imposer a priori.</p>` },
        "transmla": { title: "TransMLA : la flèche GQA → MLA", body: `<p>TransMLA<a class="cite" data-cite="8" href="#source-8">8</a> prouve que GQA est un cas restreint de MLA : tout modèle GQA se réécrit en MLA de même taille de cache, avec une expressivité en réserve. La flèche figure la conversion post-hoc — sur LLaMA-2-7B, −93 % de cache et ×10,6 d'inférence à 8K, pour seulement 6 Md tokens de réentraînement. L'écosystème GQA devient un vivier de modèles convertibles.</p>` },
        "mha": { title: "MHA — la référence", body: `<p>MHA fixe le haut de la frontière fixe : la meilleure qualité de la famille par partage de têtes, mais au cache maximal (32 768 éléments/token). Toute la question de MLA est de dépasser ce plafond de qualité tout en consommant une fraction de cette mémoire — ce que l'ablation DeepSeek-V2 confirme.</p>` },
        "mqa": { title: "MQA — le coin sacrifié", body: `<p>MQA occupe le coin bas-gauche : le plus petit cache, mais la qualité la plus faible et une instabilité d'entraînement. Il borne le compromis GQA du côté agressif. MLA atteint une taille de cache comparable <em>sans</em> le sacrifice de qualité, ce qui le rend qualitativement différent d'un simple partage de têtes poussé à l'extrême.</p>` }
      },
      "schema-06": {
        "deepseek-v3": { title: "DeepSeek-V3 — MLA à l'échelle", body: `<p>DeepSeek-V3<a class="cite" data-cite="6" href="#source-6">6</a> généralise MLA à un modèle mélange d'experts de 671 Md de paramètres, dont 37 Md activés par token. Les dimensions de son attention latente — <em>d_c</em> = 512, 128 têtes, RoPE découplé 64 — sont devenues un standard de fait : 576 éléments de cache par token et par couche. MLA n'est plus une curiosité de papier mais la brique mémoire d'un des modèles frontière les plus servis de 2025.</p>` },
        "flashmla": { title: "FlashMLA — le kernel", body: `<p>Ouvert par DeepSeek en février 2025<a class="cite" data-cite="7" href="#source-7">7</a>, FlashMLA est le kernel de décodage MLA pour GPU Hopper. Au lancement : ~3 000 Go/s en régime memory-bound et ~580 TFLOPS en régime compute-bound sur H800. Le README a depuis évolué (≈660 TFLOPS dense, variante Sparse MLA, support B200). Sans un tel kernel, le calcul-dans-le-latent resterait théorique.</p>` },
        "paged-cache": { title: "Cache paginé, blocs de 64", body: `<p>FlashMLA alloue le cache latent par <strong>pages de 64 tokens</strong>, dans la lignée de PagedAttention : plus de sur-réservation contiguë, la fragmentation tombe à quelques pourcents. Combiné à la compression MLA, cela signifie que davantage de requêtes concurrentes tiennent en mémoire haute bande passante — le déterminant du débit en service par lots.</p>` },
        "service": { title: "Effet systémique sur le service", body: `<p>Un cache 10 à 20× plus petit desserre trois contraintes d'un coup : la <strong>mémoire</strong> (plus de requêtes concurrentes), le <strong>débit</strong> (moins d'octets relus par token, en régime memory-bound), et le <strong>réseau</strong> — la <a href="../desagregation-prefill-decode/">désagrégation prefill/decode</a> transfère un latent de 576 éléments au lieu d'un cache plein. MLA n'est donc pas une optimisation locale : elle réduit simultanément trois postes que les dossiers voisins traitaient séparément.</p>` }
      },
      "schema-07": {
        "sparse": { title: "Co-conception avec la parcimonie", body: `<p>MLA compresse dans l'espace des <em>canaux</em> (le latent) ; l'<a href="../attention-parcimonieuse/">attention parcimonieuse native</a> compresse dans l'espace des <em>tokens</em>. Orthogonales et composables : DeepSeek-V3.2-Exp couple une attention parcimoniée à MLA<a class="cite" data-cite="9" href="#source-9">9</a>, remontant les deux décisions à l'entraînement. La <em>double parcimonie</em> — creux en têtes latentes et creux en tokens — est le chantier système le plus riche de 2026.</p>` },
        "quant": { title: "Quantification du latent", body: `<p>Le cache MLA est déjà un ordre de grandeur plus petit ; le <a href="../quantification-llm/">quantifier</a> le réduit encore. Mais le latent conjoint est fragile : une erreur de quantification s'y propage à <em>toutes</em> les têtes reconstruites, là où un K/V plein isole l'erreur. Le couplage MLA × quantification n'est pas additif et demande une calibration dédiée — un terrain de recherche ouvert.</p>` },
        "conversion": { title: "Conversion universelle", body: `<p>TransMLA<a class="cite" data-cite="8" href="#source-8">8</a> rend potentiellement convertible tout l'écosystème GQA (Llama, Qwen, Mistral) en MLA post-hoc, sans réentraînement complet. Si cette conversion se banalise et que l'outillage (kernels, formats de poids) suit, le principal frein à l'adoption de MLA tombe — et GQA devient un socle transitoire plutôt qu'un point d'arrivée.</p>` },
        "tensions": { title: "Tensions restantes", body: `<p>Deux frictions demeurent. Le <strong>parallélisme de tenseur</strong> : le latent unique de MLA n'est pas partitionnable, si bien qu'en décodage réparti chaque GPU charge redondamment le cache complet — des travaux récents<a class="cite" data-cite="11" href="#source-11">11</a> documentent ce trafic comme la limite structurelle de MLA. Et la <strong>complexité d'entraînement</strong> : découplage RoPE, absorption, stabilité du latent. L'adoption dépend moins de la qualité — établie — que de l'inertie de l'écosystème GQA.</p>` }
      }
    }
